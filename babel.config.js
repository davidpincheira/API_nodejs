const presets = [
    '@babel/preset-env'
];

module.exports = {presets} /* {
     api.cache(true);
  
    const presets = [ ... ];
    const plugins = [ ... ]; 
  
    return {
      presets,
      plugins
    }; 
  } */